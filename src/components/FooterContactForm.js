import React from 'react'

const FooterContactForm = ({ className }) => {
    return (
        <div className={className}>
            Contact Form will be here
        </div>
    )
}

export default FooterContactForm
